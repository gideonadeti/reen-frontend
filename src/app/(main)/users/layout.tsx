export const metadata = {
  title: {
    default: "Users",
    template: "%s | REEN",
  },
};

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default Layout;
